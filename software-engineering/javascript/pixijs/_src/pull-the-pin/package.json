{
  "name": "pull-the-pin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run dev",
    "build": "npm run lint && tsc && vite build",
    "lint": "eslint .",
    "dev": "vite",
    "build:html": "npm run lint && tsc && vite build",
    "build:zip": "zip -j index.zip dist/index.html",
    "build:ad": "npm run build:html && npm run build:zip"
  },
  "dependencies": {
    "pixi.js": "^8.8.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "eslint": "^9.21.0",
    "typescript": "~5.7.3",
    "typescript-eslint": "^8.25.0",
    "vite": "^6.2.0",
    "vite-plugin-singlefile": "^2.2.0"
  }
}
