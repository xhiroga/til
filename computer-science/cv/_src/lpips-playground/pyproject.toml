[project]
name = "lpips-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.7"
dependencies = [
    "lpips>=0.1.4",
    "torchvision>=0.20.1",
]

[tool.uv]
dev-dependencies = []
