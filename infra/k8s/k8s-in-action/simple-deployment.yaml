apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo
  labels:
    app: echo
spec:
  replicas: 4
  selector:
    matchLabels:
      app: echo
  template:
    metadata:
      labels:
        app: echo
    spec:
      containers:
        - name: nginx
          image: gihyodocker/nginx-proxy:latest
          env:
            - name: BACKEND_HOST
              value: "8080"
          ports:
            - containerPort: 80
          resources:
            limits:
              memory: "10Mi"
              cpu: "25m"
        - name: echo
          image: gihyodocker/echo:patched
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: "10Mi"
              cpu: "25m"
