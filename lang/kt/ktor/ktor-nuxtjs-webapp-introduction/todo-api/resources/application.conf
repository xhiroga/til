db{
    jdbcUrl = "jdbc:postgresql://localhost:5432/todo"
    dbUser = "ktoruser"
    dbPassword = "ktorpass"
}
ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.todo.example.ApplicationKt.module ]
    }
}
